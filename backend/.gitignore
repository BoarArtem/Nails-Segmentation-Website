.venv/
__pycache__/
*.pyc
.env
.pytest_cache/
*.egg-info/
.coverage
htmlcov/

# Model weights (app/ml/weights/best.pt) are tracked directly in git: Railway
# builds the Docker image straight from this repo, so the weights must be
# present in the checkout. If the model grows much larger, move to Git LFS.
